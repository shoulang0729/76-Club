# 設計：ドロワー廃止 → ホーム配下の固定サイドメニュー — 2026-08-29 確定

対象: `index.html` / `js/nav.js` / `js/home.js` / `styles.css`。**表示・導線のみ**の変更（§3計算・データモデル§4・localStorage キー集合に非接触＝正本 2026-07-12 への追記は不要）。
基準コミット: c773865（1on1 移設まで反映済みの作業ツリー。行番号は全てこの時点。**並行の 1on1 作業が styles.css/i18n.js を触るため、実装時は行番号でなくセレクタ/キー名で位置特定すること**）。

ユーザーフィードバック（原文）: 「サイドメニューはOK、ドロワーでなくホーム配下に固定したメニューにして」
→ PR #58 のオーバーレイ式ドロワー（☰ボタン開閉）を**廃止**し、**ホーム画面内に常時見える固定メニュー**（広幅=左カラム・狭幅=最上部）を置く。

## 0. 既存設計との上書き関係（必読）

| 既存節 | 内容 | 本設計での扱い |
|---|---|---|
| `2026-08-20-home-drawer.md` §1 | ☰＋左スライドドロワー＋オーバーレイ | **廃止（本設計 §1 が上書き）**。「2タブ（ホーム/結果発表）」「`#navCur` 現在地表示」「結果発表はメニューに入れない」「開閉/表示状態は揮発」は**存続** |
| 同 §2.1 | `#menuBtn`（☰）・`#navCur` の onclick=drawerToggle | **上書き**: ☰削除・navCur は**表示専用**（onclick なし）＝本設計 §3 |
| 同 §2.2 / §3(drawerToggle) / §5.2 | ドロワー DOM / drawerToggle / ドロワー CSS | **全廃**（削除一覧＝本設計 §5.1） |
| 同 §4 | ホームの「☰ メニュー」ワイドボタン | **廃止**（固定メニューが常時見えるため不要）＝本設計 §2 |
| 同 §6 | i18n（home.d.result 削除・文言ポリッシュ） | **実装済み・存続**（本設計での増減はゼロ） |
| `2026-08-20-game-split.md` §4.1 | ドロワー5項目化（基本設定→ゲーム設定→コース→選手→スコア） | **項目リスト（5項目・順序・タイトル＋説明の2行・結果発表なし）は存続**し、置き場所を**ホーム固定メニュー**に変更（本設計 §2.2）。ドロワー前提の DOM 記述（`<button data-tab=… data-i18n=…>`）は失効＝home.js の JS テンプレートに置き換え |
| 同 §2 | ホーム α/β 50:50（`.chwrap`） | **存続**。ただし置き場所は**右カラム（.homemain）内で50:50**（全幅帯にしない）＝本設計 §2.1。CSS はあちらの §2 をそのまま採用 |
| 同 §5.2/§5.4 | `WORK_TABS` に basic 追加・起動タブ 'basic' | **存続**（navCur のラベル表示・起動画面として引き続き必要） |
| 同 §11 | 推奨PR分割（PR-①=chwrap / PR-②=分割本体） | **本設計 §9 が上書き**（chwrap は本件PRに吸収・PR-①は消滅） |
| 正本 §11.14 | 投影前提の表示原則 | ホームは幹事の作業画面＝通常UIで可。原則4のみ準拠（メニューに開閉状態なし・保存キー増ゼロ） |

## 1. 結論サマリ（前→後）

- **前（PR #58）**: ナビ=［☰］ホーム｜結果発表｜#navCur。☰/navCur タップで左スライドドロワー（4項目）＋オーバーレイ。ホームのガイドカード末尾に「☰ メニュー」ワイドボタン。
- **後**:
  - **ナビ= ホーム｜結果発表｜#navCur（表示専用）** の2タブ＋現在地表示。☰ボタン・ドロワー・オーバーレイ・drawerToggle・「☰ メニュー」ボタンを**全部削除**。
  - **ホームを2カラム化**（`.homegrid`）: 左=**固定メニュー**（カード型・縦並び）、右=既存コンテンツ（ガイド→α/β解説→α/βカード→skip チェック）。**760px 以下は1カラム縦積み（メニューが上）**。
  - **メニュー項目**（game-split §4.1 の5項目・順序固定）: **基本設定→ゲーム設定→コース→選手→スコア**。各項目=タイトル（`nav.*`）＋説明（`home.d.*`）の2行。結果発表は入れない（上部タブ）。※#61 実装前に本件を先行させる場合は暫定4項目（ゲーム→コース→選手→スコア）＝配列1行の差。
  - **α/β カードは右カラム内で左右50:50**（`.chwrap`、game-split §2 の CSS そのまま。560px 以下で縦積み）。
  - **作業画面からの横移動**: 上部「ホーム」タブ（sticky で常時可視）→ メニュー項目、の2タップ。旧ドロワー（☰→項目）と同じタップ数で導線の損失なし。`#navCur` は現在画面名の**表示専用**（タップで何も起きない・cursor:default）。
  - i18n **増減ゼロ**（`home.menu`=メニュー見出し・`nav.*`・`home.d.*` を再利用）。localStorage 非接触。

## 2. ホーム（js/home.js）

### 2.1 renderHome の構造（前→後）

```js
const HOME_MENU=['game','course','players','score'];   // #61 実装後は ['basic','game','course','players','score']（§0 の存続項目）
function renderHome(){
  const el=document.getElementById('view-home');
  const chCard=(c)=>{ …（現13–21行のまま・不変）… };
  const mi=tab=>`<button class="hm-item" onclick="go('${tab}')">
    <span class="hm-t">${t('nav.'+tab)}</span><span class="hm-d">${t('home.d.'+tab)}</span></button>`;
  el.innerHTML = `<div class="homegrid">
    <nav class="card homemenu" aria-label="${t('home.menu')}">
      <h2>${t('home.menu')}</h2>
      ${HOME_MENU.map(mi).join('')}
    </nav>
    <div class="homemain">
      <div class="card hometop">…（現23–28行のガイド。現29行の「☰ メニュー」ボタン行は削除）…</div>
      <div class="card">…（ch.title カード・現31–34行のまま）…</div>
      <div class="chwrap">${chCard('a')}${chCard('b')}</div>
      <label class="tgl seentop">…（現36行のまま）…</label>
    </div>
  </div>`;
}
```

- **変更点は3つだけ**: (a) `HOME_MENU`＋`mi` でメニューカードを新設し `.homegrid` 左に置く、(b) 現29行の `drawerToggle(true)` ボタン行を削除、(c) 現35行のラッパ `rank-wrap` → `chwrap`（game-split §2 と同一変更。`.rank-wrap` は結果発表専用に戻る）。
- ガイド4ステップ・ALPHA_GAMES/BETA_GAMES・chCard・seenTop/setSeenTop は**不変**。
- メニューは innerHTML 生成＝`setLang→render→renderHome` で3言語追従（data-i18n 不要）。DOM 順は メニュー→ガイド なので、狭幅縦積みでもメニューが上（launcher として機能）。
- **メニュー項目に active 表示は不要**（メニューが見えるのはホーム表示中のみ＝どの作業画面も「現在地」になり得ないため。既定として確定）。

### 2.2 メニュー項目（#61 実装後の完成形・順序固定）

| # | tab | タイトル（nav.*） | 説明（home.d.*） |
|---|---|---|---|
| 1 | basic | 基本設定 | コンペの作成・選択・日付・コース名 |
| 2 | game | ゲーム設定 | ペリア設定・集計するゲーム・賞金ポイント配点 |
| 3 | course | コース | パー・隠しホール・ニアピン/ドラコン対象ホール |
| 4 | players | 選手 | 選手の登録・参加者とチームの設定 |
| 5 | score | スコア | ラウンド中の打数入力 |

（1・2行目のキー・文言は game-split §6.1/§6.2 で定義済み。本設計での新キーはなし。）

## 3. ナビ（index.html / js/nav.js）

### 3.1 index.html（35行と40–47行を削除・38行を1属性変更）

```html
<nav id="mainNav" class="mainnav">
  <button data-tab="home" onclick="go('home')" data-i18n="nav.home">ホーム</button>
  <button data-tab="result" onclick="go('result')" data-i18n="nav.result">結果発表</button>
  <button id="navCur" style="display:none"></button>
</nav>
```
- 削除: 35行 `#menuBtn`（☰）／ 40行 `#drawerOverlay`／ 41–47行 `#appDrawer` 一式。
- `#navCur`: `onclick="drawerToggle()"` を**外して表示専用に**（現在画面名＋下線。押しても何も起きない）。タップでホームへ飛ばす案は不採用＝「現在画面のラベルを押すと別画面へ移動」は予測に反し、ホームタブが常時1タップ圏にあるため冗長（既定として確定）。
- `<main>`（view-*）・script 読込順は**変更しない**（view-basic/basic.js の追加は #61=game-split §4.2/§4.3 の管轄）。

### 3.2 js/nav.js（3箇所）

1. **削除**: 48–51行（コメント＋`drawerToggle` 関数まるごと）。グローバル参照は index.html/home.js の削除分で全て消える（残存参照ゼロにすること）。
2. **`go()`（53行）**: `function go(tab){ rlStopTimer(); activeTab=tab; render(); }`（`drawerToggle(false)` の1呼び出しを除去。activeTab→render の切替機構は不変）。
3. **`render()`（60行）**: セレクタから `#appDrawer` を除去 → `document.querySelectorAll('#mainNav button').forEach(b=>b.classList.toggle('on', b.dataset.tab===activeTab));`
- **不変**: `WORK_TABS`（52行。#61 で basic 追加＝あちらの管轄）・navCur 更新の3行（58–59行＝isWork 判定・textContent 設定はそのまま生かす）・`views` マップ・`SC_NARROW_MQ`・rlStopTimer・renderXxx 分岐・init.js。

## 4. CSS（styles.css）

### 4.1 削除（ドロワー資産一式）

- 66行 `.mainnav .menubtn`
- 67行 `.drawer-overlay`
- 68–69行 `.drawer`
- 70–71行 `.drawer-h`
- 72–77行 `.drawer button` / `.hl-t` / `.hl-d` / `.on` 系 6ルール
- 78行 `@media(min-width:1024px){ .drawer… .menubtn… }` 行まるごと
- 59行コメントを更新: 「主ナビ: ホーム/結果発表の2タブ＋現在地 #navCur（表示専用）。作業画面へはホームの固定メニューから（2026-08-20-home-menu.md）」

### 4.2 新設（ホーム画面ブロック＝`.hometop` 付近に追加）

```css
/* ホーム固定メニュー（2026-08-20-home-menu.md）: ドロワー廃止。広幅=左カラム固定・760px以下=最上部に縦積み */
.homegrid{display:grid;grid-template-columns:260px minmax(0,1fr);gap:12px;align-items:start}
.homemenu{margin-bottom:0;padding:12px 8px}
.homemenu h2{padding:2px 10px 8px}
.hm-item{display:flex;flex-direction:column;gap:3px;width:100%;text-align:left;border:0;background:transparent;
  border-radius:var(--r-sm);padding:12px 10px;cursor:pointer}
.hm-item:active{background:var(--hl-bg)}
.hm-item .hm-t{font-size:var(--f-title);font-weight:var(--w-bold);color:var(--strong)}
.hm-item .hm-d{font-size:var(--f-small);color:var(--sub);line-height:1.4}
#navCur{cursor:default}
@media(max-width:760px){ .homegrid{grid-template-columns:1fr} }
@media(min-width:1024px){ .hm-item{padding:14px 12px} }
/* ホームのα/β 2カラム（右カラム内で左右50:50・560px以下で縦積み。game-split §2 の CSS を無改変で採用） */
.chwrap{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:12px}
.chwrap>.card{margin-bottom:0;display:flex;flex-direction:column}
.chwrap>.card>div:last-child{margin-top:auto}
@media(max-width:560px){ .chwrap{grid-template-columns:1fr} }
```

- **設計意図**: メニューは `.card`（白面＋罫線）でガイドカードと同格の見え方。項目の見た目（2行構成・タイトル --f-title 太字・説明 --f-small・タップ中 --hl-bg）は旧ドロワー項目を**そのまま踏襲**（=「サイドメニューはOK」の評価点を維持）。トークンのみ使用＝ライト/ダーク自動反転。濃色ベタ塗りなし。
- 左カラム 260px は旧ドロワー幅 min(78vw,300px) より控えめ＝右カラム（960px コンテナ時 約664px）で chwrap 50:50 が各約327px を確保。
- メニューは**非 sticky**（ホームは縦に短く、追従の複雑さに見合わない。既定として確定）。
- ブレークポイントは既存値を再利用: 2カラム⇄縦積み=**760px**（.ind-split と同値）／chwrap=**560px**（game-split §2 どおり）。

### 4.3 幅の検算

- **ナビ @320px**（☰が消えて前より余裕）: ja=ホーム46＋結果発表72＋navCur最長「基本設定」(4字52+20=72)≈190＋gap12＋pad24=**226px ✓**／en=Home49＋Results70＋Players70≈189＋36=**225px ✓**（zh はさらに短い）。
- **ホーム @320px**: 1カラム。hm-item は2行で高さ≈57px（15+3+15＋pad24）≥48px タッチターゲット ✓。
- **ホーム @761–1023px**: 左260＋右 約465–730px → chwrap 各カード約227px〜（pill は折返しで吸収）✓。
- **@1024px**: hm-item padding 14px 12px（高さ≈61px）・mainnav button min-height:48px（既存92行・不変）✓。

## 5. i18n（js/i18n.js）

- **追加 0・削除 0・値変更 0**。`home.menu`（メニューカード見出し）・`nav.game/course/players/score`・`home.d.game/course/players/score` を再利用。
- #61 側の追加2キー（`nav.basic`/`home.d.basic`）・値変更9キー（game-split §6）は**あちらの管轄のまま有効**（固定メニュー化と無矛盾＝文言に「ドロワー/☰」への言及なし）。

## 6. 挙動比較（前→後・1行例）

- 起動（seenTop 未設定）: 前後ともホーム。前=ガイド＋「☰ メニュー」ボタン → 後=左に固定メニュー（狭幅は最上部）＋右にガイド。メニューが常時見えるので初見者の入口ボタンは不要に。
- 起動（seenTop=1）: 前後とも作業画面＋navCur 点灯（#61 後は基本設定）。後は navCur を押しても何も起きない（表示専用）。
- Par を変えたい（ホームから）: 前「☰→コース」→ 後「（見えている）コース」**1タップに短縮**。
- Par を変えたい（スコア画面から）: 前「☰→コース」2タップ → 後「ホーム→コース」2タップ（同数・導線の損失なし）。
- 発表から入力修正: 前「☰→スコア」→ 後「ホーム→スコア」。結果発表への遷移は前後とも上部タブ1タップ（不変）。
- リロード: 前=ドロワー閉で起動 → 後=開閉状態そのものが存在しない（保存キー増ゼロ・`golfCompe_v1` 不変）。
- 結果発表の投影中: ナビ=「ホーム｜結果発表(下線)」のみ。☰が消えて前よりさらに静か（§11.14 に沿う）。

## 7. 触らない範囲（load-bearing）

§3 計算・`state`/`golfCompe_v1` と localStorage 全キー（**キー追加もなし**）・`views`/activeTab/render の切替機構・`WORK_TABS` と navCur の isWork 判定ロジック・各画面の中身（renderGame/renderCourse/renderPlayers/renderScore/renderResult）・ガイド4ステップと seenTop・script 読込順・inline onclick / 非ESM・ヘッダ（chBadge/テーマ/言語）・`.rank-wrap`（結果発表用）・`tab.*` キー温存（N-4）・**並行作業中の js/results.js・js/players.js・js/i18n.js（1on1 移設）には触れない**。

## 8. 受け入れ条件

1. `node tools/verify.mjs` 全パス（i18n 3言語キー集合一致・未定義キー参照0・CSS孤立var()なし・計算回帰 #3/Vegas 一致）。
2. ナビが「ホーム｜結果発表（＋作業画面中のみ navCur）」になり、☰ボタン・ドロワー・オーバーレイが DOM/CSS/JS から完全に消えている（`drawerToggle`/`menubtn`/`drawer`/`hl-t`/`hl-d` の残存参照ゼロ）。
3. ホームが広幅で左=メニュー・右=ガイド→α/β解説→α/βカード→skip の2カラム。760px 以下で1カラム（メニューが最上部）。
4. メニュー項目は上から**基本設定/ゲーム設定/コース/選手/スコア**（#61 統合時。先行実装時は ゲーム/コース/選手/スコア の4項目）で結果発表なし。各項目=タイトル＋説明の2行、タップで該当画面へ遷移。3言語でラベル追従。
5. navCur は作業画面で現在画面名＋下線を表示し、タップしても何も起きない（cursor:default）。ホーム/結果発表では非表示。
6. α/β カードが右カラム内で左右50:50・等高・「この版を使う」下端揃え、560px 以下で縦積み（α上）。`ch.enter`/`ch.current` の動作不変。
7. ホームの「☰ メニュー」ワイドボタンが消えている。ガイド4ステップ・skip チェックは従来どおり。
8. 320px（ja/zh/en）でナビ折返し・横スクロールなし。メニュー項目のタッチ高さ 48px 以上（1024px でも）。ライト/ダーク両テーマ非破綻。
9. localStorage のキー集合・`golfCompe_v1` の JSON が前後で不変。リロードで表示崩れなし。
10. i18n キー集合が本件差分でゼロ増減（#61 統合分は game-split §6 のとおり）。

## 9. 推奨PR構成（#61 との統合・順序）

**推奨: 2PR 直列（本件が先行）。いずれも進行中の 1on1 PR（js/results.js・js/players.js・js/i18n.js・styles.css）のマージ後に着手**（styles.css/i18n.js の競合回避。CLAUDE.md「同じファイル群は直列」）。

1. **PR-A（本件）: ドロワー廃止＋ホーム固定メニュー（4項目）＋chwrap 50:50**
   - files: index.html・js/nav.js・js/home.js・styles.css。i18n 増減ゼロ。
   - game-split の旧 PR-①（chwrap）を**本PRに吸収**（renderHome を書き換えるついでに rank-wrap→chwrap。二度触りを回避）。
   - ユーザーフィードバック起因の作り直しなので最短で出荷する。
2. **PR-B（#61）: 基本設定/ゲーム設定の画面分割＋メニュー5項目化**
   - game-split §3–§7 のとおり（basic.js 新設・view-basic・init 'basic'・i18n +2/値9・CLAUDE.md 読込順）。ただし **§4.1 のドロワー DOM 編集は読み替え**: home.js の `HOME_MENU` に `'basic'` を先頭追加する**1行**＋nav.js `WORK_TABS` に basic 追加、で足りる（ドロワーはもう存在しない）。
   - PR-A で捨てる資産を PR-B が作らない順序（逆順だと5項目ドロワーを作って即削除する無駄が出る）。

代替案（1PR 統合=A+B を一括）も成立するが、変更ファイル9個超の大PRになりレビュー・切り戻し単位が粗くなるため次点。
